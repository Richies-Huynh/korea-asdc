rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Proof frames are written by the Admin SDK and served through the
    // authenticated /api/detections/[id]/file route, so deny all direct
    // client access.
    match /detections/{id} {
      allow read, write: if false;
    }
  }
}
